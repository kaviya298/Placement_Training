import java.util.*;
class Solution {
    static int evenlyDivides(int n) {
        int k=n;
        int count=0;
        while(n>0){
            int digit=n%10;
            n=n/10;
            if(digit!=0 && k%digit==0){
                count++;
            }
        }
        return count;
}
}
